---
import { formatDate } from '@/helpers';

interface Props {
  date: string;
}

const { date } = Astro.props;
---

<p class="text-sm">Escrito en: <span class="text-coffee-600">{formatDate(date)}</span></p>
